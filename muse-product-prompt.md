# Muse —— 美学灵感社区 App · 低代码平台产品生成提示词（含验收边界）

> 用途：将本文档完整提供给低代码 / AI 应用生成平台（如阿里秒悟、钉钉宜搭、轻流、简道云 AI、百度爱速搭等），用于生成与本产品同概念的应用。文档自包含、平台无关，全部技术描述均已转换为低代码概念（数据表、接口编排、AI 节点、页面画布、主题配置）。

---

## 一、产品定位（一句话）

**Muse** 是一个"美学灵感社区"：用户浏览瀑布流灵感内容、以 Tinder 式滑动卡片探索每日精选、发布自己的图文灵感、收藏关注、并与创作者私信交流。大模型 AI 深度嵌入"创作灵感生成"与"私信智能回复"两个场景。

- 目标用户：热爱生活方式、艺术、设计、摄影、旅行的内容消费者与创作者
- 产品气质：安静、高级、玻璃拟态（Glassmorphism）、蓝紫主色、动效细腻
- 界面语言：中文为主，部分品牌词保留英文（Muse、Daily Muse、Explore 等）
- 终端形态：**PC Web + 移动端 H5 双端自适应**（移动端为主要体验）

---

## 二、低代码平台概念映射（如何把本产品落到你的平台上）

| 本产品需要的能力 | 低代码平台对应物 | 说明 |
|---|---|---|
| 页面与导航 | 页面画布 / 路由 Tab 状态 | 共 10 个页面，用全局状态变量（currentTab）切换，无需真实路由也可 |
| 数据存储 | 平台数据表（自建表） | 6 张表，见第六章；首次使用时需初始化并导入种子数据 |
| 后端接口 | 接口编排 / 数据源服务 / 云函数 | 12 个接口，见第七章；平台若支持"页面直连数据表"，可简化为数据表 CRUD |
| AI 能力 | 平台内置大模型节点 / LLM 连接器 | 2 个 AI 节点，见第八章；**必须配置失败兜底分支** |
| 样式主题 | 主题变量 + 自定义 CSS（若平台支持） | 玻璃拟态需自定义 CSS 块，见第三章；不支持时用"白色半透明底 + 模糊/阴影组件样式"近似 |
| 图片资源 | 外链图片 URL | 种子数据用稳定占位图（如 picsum.photos），不涉及文件上传 |
| 登录 | 模拟登录（单用户） | 不做真实鉴权，见 5.1 |

**平台能力降级原则**：某项能力平台不支持时（如滑动拖拽手势、长按手势），允许用等效交互替代（按钮点击替代滑动、"收藏标签"按钮替代长按），但必须在验收说明中列出替代项，且功能语义不变。

---

## 三、设计系统（主题配置 + 自定义样式）

### 3.1 色彩令牌

| 用途 | 亮色模式 | 暗色模式 |
|---|---|---|
| 页面背景 | `#f8f9ff` | `#0b1c30` |
| 主文字 | `#0b1c30` | 浅灰 `gray-100` |
| 次要文字 | `#424754` | `gray-300` |
| 品牌主色 | `#0058be`（深）/ `#2170e4`（亮），主按钮用两色横向渐变 | 强调文字 `#adc6ff` |
| 点赞/爱心 | `#a43073` / `#fc79bd` | 同左 |
| 标签底色 | `#eff4ff` | 深灰蓝 |

### 3.2 玻璃拟态三件套（核心视觉语言，自定义 CSS 实现）

- **glass-panel**：`rgba(255,255,255,0.72)` + `backdrop-filter: blur(20px)` + 白色半透明描边 + 柔和阴影；暗色下 `rgba(30,41,59,0.65)`
- **glass-card**：同上，透明度 0.75、模糊 16px，用于卡片
- **glass-button**：透明度 0.55、模糊 12px，hover 时透明度升至 0.85

若平台不支持 `backdrop-filter`：用 85% 不透明白底 + 浅阴影近似，视觉允许降级但不允许破版。

### 3.3 其他规范

- 圆角：卡片大圆角（16~24px），按钮/标签全圆角胶囊
- 字体：正文 Inter（或平台默认无衬线），标题 Quicksand（或加粗圆体感字体）
- 图标：Material Symbols 图标集（或平台内置图标库），选中态优先用实心图标
- 暗色模式：全局开关变量驱动（Header 与设置页两处入口），所有容器需有暗色变体
- 动效基调：卡片 hover 轻微上浮、图片 hover 缓慢放大、按钮按压缩小 95%、Toast 3 秒自动消失

---

## 四、全局导航与布局

### 4.1 PC 端（≥768px）

- 顶部固定导航栏（高约 80px，半透明毛玻璃）：左侧 Logo（气泡图标 + "Muse"），中部 6 个 Tab（Discovery / Explore / Create / Bookmarks / Messages / Profile，当前项高亮），右侧主题切换 + 设置
- 内容区最大宽度 1200~1440px 居中

### 4.2 移动端（<768px）

- 顶部固定栏（高约 64px）：左菜单按钮（打开侧边抽屉）、中间 "Muse" 标题（点击回发现页）、右侧主题切换 + 设置
- **底部悬浮胶囊导航栏**：距底部约 24px、毛玻璃圆角胶囊；四项：探索 / 关注 / 聊天 / 我，当前项高亮
- **侧边抽屉菜单**：左滑出，含用户信息、全部页面入口、主题切换，带半透明遮罩
- 文章详情页与设置页隐藏底部导航

### 4.3 全局状态变量

至少需要：`currentTab`（当前页面）、`isDarkMode`（暗色开关）、`currentUser`（当前用户）、`selectedPost`（打开的文章，非空时覆盖渲染详情页）。

---

## 五、页面与功能详述（共 10 个页面）

### 5.0 加载与异常

- 首屏数据加载期间显示品牌 Loading（Logo + "Loading Muse..." 脉冲动画）
- 所有数据操作失败统一 Toast 报错，不允许静默失败（AI 节点除外，见降级策略）

### 5.1 登录页（模拟）

- 渐变背景（蓝→淡紫）居中玻璃卡片：品牌图形、"Muse" 标题、副标题 "Discover Beauty. Share Life."
- 手机号输入：国家码下拉（+86/+1/+81/+44）+ 手机号
- 验证码：点击 "Get Code" 后**模拟发送**（固定验证码 888888 自动填入），按钮变 "Resend"
- 用户协议勾选框（默认勾选）；未勾选点击登录时 Toast 报错拦截
- 登录按钮 → 进入发现页。**无需真实鉴权，单用户模拟登录即可**

### 5.2 发现页（Discovery）

1. **Hero 区**："Daily Muse" 每日精选横幅（移动高 360px / PC 高 480px），全宽大图 + 底部深色渐变遮罩 + 玻璃文案面板（徽章、标题、导语、"Read Curation" 入口→打开某篇文章详情）
2. **瀑布流**：移动 2 列 / 平板 3 列 / PC 4 列（可用多列布局或栅格模拟），卡片两种形态：
   - **图文卡**：圆角封面图（右上角收藏按钮，收藏态实心）、最多 2 个标签、标题、底部作者头像+名字（左）与点赞按钮（右）
   - **引用卡**：无图，紫色底引号图标 + 大字引言 + 作者与点赞
3. **标签收藏**：长按标签约 450ms 触发抖动动画并加星标收藏/取消，Toast 反馈（平台不支持长按手势时，可用标签上的小星标按钮替代，语义不变）
4. 点击卡片进入文章详情；底部 "Load More" 按钮（可先做静态占位）

### 5.3 探索页（Explore）—— 核心差异化功能

**PC 端 = 滑动卡片牌堆（Tinder 式）**：

- 牌堆区高约 520~580px；顶卡可水平拖拽并轻微旋转，背后下一张卡随拖拽逐渐放大浮现
- **两种滑动模式切换器**（胶囊分段控件）：
  - `Next` 模式：向左/向右滑动都只是前进到下一张
  - `Like` 模式（默认）：右滑 = 喜欢（绿色 LIKE 徽章渐显），左滑 = 跳过（红色 PASS 徽章）
- 飞出后中央爆发动画反馈：like 显示粉色实心爱心、pass 显示灰色叉
- 卡片内容：全屏封面图、顶部类别徽章 + "查看详情"按钮、底部渐变遮罩内作者信息、标题、两行摘要、标签
- 底部操作按钮排：跳过/上一张、查看文章、私信作者（跳转消息页）、喜欢/下一张（主色大圆钮）
- 卡片循环播放，显示 `当前序号 / 总数`
- **平台不支持拖拽手势时**：用左右箭头按钮 + 喜欢/跳过按钮实现等效流程，LIKE/PASS 语义与反馈动画保留

**移动端 = 双模式探索页**：分段切换器，可在"瀑布流"（复用发现页瀑布流）与"卡片滑动"（复用上述牌堆）之间切换，带淡入过渡动画

### 5.4 发布页（Create）

表单字段（玻璃面板承载）：

1. 封面图 URL 输入 + 实时预览（仅支持 URL，不做文件上传）
2. 标题（必填）
3. 类别选择：药丸单选组，固定类别【生活 / AI科技 / 艺术设计 / 摄影 / 旅行 / 观点】
4. 正文多行文本
5. 话题标签：空格分隔输入，提交时自动补 `#` 前缀；为空时默认 `#Inspiration #Muse`
6. **AI Muse 灵感生成器**（紫蓝渐变按钮打开模态框）：输入主题（如"京都的清晨咖啡"）→ 调用 AI 节点 → 自动回填标题、正文（100~150 词）、3 个话题标签、建议类别；生成中显示加载态 "Inspiring..."
7. 发布成功：新帖置顶插入发现页、个人帖数 +1、跳转发现页、Toast "Post published!"

### 5.5 文章详情页

- 浮动返回按钮（毛玻璃圆钮）
- 头图区 + 底部渐变遮罩：类别徽章 + 大标题
- 作者信息卡：头像、名字、阅读时长与日期、"Follow" 按钮（可静态）
- 正文区：段落 + 高亮引用块（渐变底、左侧主色竖线、斜体引言）+ 横向滚动画廊（多图时）
- 互动栏：点赞（计数）、评论数、收藏、分享（复制链接 + Toast）
- 评论区：输入框 + 提交后新评论置顶插入、评论列表（作者名高亮）

### 5.6 消息页（Messages）—— AI 智能回复

- 单会话场景：与创作者 "Elena Rivera"（视觉艺术家/旅行摄影师）聊天
- 顶栏：对方头像 + 绿色在线点、活跃状态、通话/视频占位按钮
- 消息气泡：我方右侧主色渐变气泡，对方左侧白色气泡，下方时间戳
- 发送后显示 "{对方名字} is typing..." 指示 → 调用 AI 节点以对方人格生成 1~3 句温暖对话式回复
- **AI 降级铁律**：AI 节点失败或未配置时，自动回复兜底文案（如 "Thanks for your note! I'd love to chat more soon."），流程不中断
- 输入栏：附件、表情占位按钮 + 发送按钮（空内容禁用）
- 所有消息写入数据表，刷新后仍在

### 5.7 关注/收藏页（Bookmarks）

- 标题 "关注" + 副标题 "收藏的帖子与灵感清单"
- 搜索框（匹配标题与标签）+ 类别药丸过滤【All / Architecture / Food & Drink / Travel / Design】，二者叠加生效
- 收藏卡片：左封面缩略图（带类别角标）、右侧来源、标题、摘要、标签、删除按钮
- 删除：数据表删除后列表即时更新 + Toast；空状态显示图标 + "暂无关注内容" + 引导文案
- 点击卡片尝试匹配打开对应文章详情

### 5.8 个人主页（Profile）

- 顶排：分享按钮（复制链接）、设置按钮
- 资料卡：渐变描边圆头像（蓝→粉渐变环）、昵称、handle、简介、"Edit Profile" 按钮打开编辑弹窗（可修改名字、bio、头像 URL，保存写数据表 + Toast）
- 数据条：Posts / Followers / Following 三项
- 内容 Tab：Posts / Saved / Appreciated（Saved/Appreciated 可为空占位）
- 作品网格：正方形卡片（移动 2 列 / PC 3~4 列），hover 显示暗色遮罩 + 标题 + 点赞数；无图帖显示渐变底 + 标题

### 5.9 设置页（Settings）

玻璃卡片分组：

1. Appearance：暗色主题开关（实时生效）
2. Notifications：推送开关（本地状态即可）
3. Language：下拉（中文简体 / English / 日本語，本地状态即可）
4. Account：红色"切换账号/退出登录"→ 回到登录页

---

## 六、数据表设计（平台数据表，共 6 张）

| 表名 | 字段 |
|---|---|
| users | id、name、handle（唯一）、avatar、bio、posts_count、followers_count、following_count、active_status、created_at |
| posts | id、author_id（关联 users）、title、content、image_url、quote、category、tags（JSON 数组）、likes_count、comments_count、reads、date、type、gallery_images（JSON 数组）、created_at |
| likes | user_id、post_id（联合唯一，toggle 语义） |
| bookmarks | id、user_id（关联 users）、title、category、source、image_url、tags、excerpt、created_at |
| messages | id、sender（枚举 user/other）、text、timestamp、created_at |
| comments | id、post_id（关联 posts）、author_name、text、created_at |

**种子数据要求**：至少 1 个当前用户（user_1）、4~6 个创作者用户、12~16 篇覆盖各类别的帖子（含至少 1 篇引用帖、2~3 篇带画廊多图的帖子）、6~8 条收藏、4~6 条历史消息。内容主题围绕极简主义、建筑、咖啡、旅行、水彩艺术等美学方向，图片使用 picsum.photos 等稳定占位图源。

---

## 七、接口/服务编排清单（接口编排或页面直连数据表均可）

| 接口语义 | 说明 |
|---|---|
| 获取帖子列表（可按 category 过滤） | 返回帖子 + 作者信息 + 当前用户 isLiked/isSaved 状态 |
| 获取探索卡片列表 | 供牌堆使用 |
| 创建帖子 | 作者为当前用户，返回新帖 |
| Toggle 点赞 | 写 likes 表 + 更新 likes_count，返回更新后的帖子 |
| 获取/新增评论 | 按 post_id |
| 获取/更新当前用户资料 | GET / PUT 语义 |
| 获取历史消息 / 发送用户消息 / 保存对方回复 | 均写 messages 表 |
| 获取收藏列表 / 删除收藏 | 按 user_id |
| AI 灵感生成 | 返回 `{title, text, tags[], suggestedCategory}`，见第八章 |
| AI 私信回复 | 入参 `{persona, lastMessage}`，返回 `{reply}`，见第八章 |

错误约定：失败时返回统一错误信息，前端 Toast 提示。若平台有统一异常处理机制则接入之。

---

## 八、AI 节点配置（平台内置大模型/连接器）

1. **灵感生成节点**：System Prompt 设定为"美学社交平台的 AI 创作伙伴"；要求输出**严格 JSON**（title / text 约 100~150 词 / tags 三个以 # 开头 / suggestedCategory 必须是预设类别之一）；输出解析失败时走 Toast 报错兜底
2. **私信回复节点**：以创作者人格（Elena Rivera：温暖、有思想、健谈、友好的视觉艺术家）回复，1~3 句、口语化；创造性略高（temperature ≈ 0.8 等效档位）
3. **降级铁律**：两个节点都必须配置失败/未配置时的兜底分支——灵感生成弹 Toast 报错，私信回复走固定兜底文案；**任何情况下不得阻塞主流程或白屏**

---

## 九、验收边界（Acceptance Criteria）

### ✅ A. 功能验收（全部必须通过）

| # | 验收项 |
|---|---|
| A1 | 登录页勾选协议后可进入主应用；未勾选被拦截并有错误提示 |
| A2 | 发现页 Hero + 瀑布流正常渲染，PC ≥3 列、移动 2 列；图文卡与引用卡两种形态均存在 |
| A3 | 点赞 toggle：图标实心/空心切换且计数 ±1，列表与详情页状态同步；刷新后持久 |
| A4 | 收藏 toggle：卡片与详情页图标状态同步 |
| A5 | 标签收藏交互（长按或等效按钮）可加星/取消，有 Toast 反馈，不误触卡片跳转 |
| A6 | 探索牌堆可切换卡片（拖拽或等效按钮），卡片循环不越界，序号指示正确 |
| A7 | Next / Like 两种模式切换即时生效；Like 模式有 LIKE/PASS 反馈与爱心/叉爆发提示 |
| A8 | 移动端探索页可在"瀑布流/卡片滑动"间切换且有过渡动画 |
| A9 | 发布流程：标题必填校验 → 发布成功 → 新帖出现在发现页首位 → Toast → 跳转发现页 |
| A10 | AI 灵感生成能回填标题/正文/标签/类别；AI 不可用时报错 Toast 且不崩溃 |
| A11 | 文章详情：正文/引用块/画廊渲染正确；评论可提交并置顶显示；分享为复制链接 |
| A12 | 私信：发送后出现 typing 指示 → 收到 AI（或兜底）回复；消息刷新后仍持久 |
| A13 | 关注页：搜索与类别过滤可叠加；删除后列表即时更新 + Toast；空态正确 |
| A14 | 个人主页：资料编辑保存后页面即时更新且持久；作品网格只显示当前用户的帖子 |
| A15 | 设置页暗色开关全站即时生效（含所有玻璃容器暗色变体）；退出回到登录页 |
| A16 | PC 顶部导航与移动底部导航/抽屉菜单按断点正确切换；文章详情与设置页隐藏底部导航 |
| A17 | 所有 Toast 三态（成功/失败/信息）正常、约 3s 自动消失；无任何操作出现未捕获异常/白屏 |

### ⚠️ B. 边界与异常场景（必须正确处理）

| # | 场景 | 期望行为 |
|---|---|---|
| B1 | AI 节点未配置/调用超时/返回格式错误 | 灵感生成 Toast 报错；私信走兜底回复；主流程不中断 |
| B2 | 数据接口失败 | 统一 error Toast，UI 保留旧数据，不崩溃不白屏 |
| B3 | 空输入提交（空标题发帖、空消息、空评论） | 前端拦截，按钮禁用或早返回 |
| B4 | 关注页搜索无结果 | 显示空态卡片而非空白 |
| B5 | 牌堆只剩 1 张卡 | 无背景卡也不报错，循环正常 |
| B6 | 图片加载失败/超长标题/超长正文 | 布局不塌陷（截断、占位或限高） |
| B7 | 连续快速点赞 | 无竞态导致计数错乱，以最后一次服务端结果为准 |
| B8 | 未勾选协议登录、国家码切换 | 拦截提示；输入框保持可用 |

### ❌ C. 明确不在范围内（不得因缺失判为不合格）

1. 真实用户体系：注册/多账号/密码登录/会话管理（登录为模拟单用户）
2. 真实短信验证码、邮箱验证
3. 图片本地上传/对象存储（仅支持 URL 引用）
4. 关注/粉丝关系真实逻辑、Follow 按钮实际生效、多会话消息列表
5. 分页无限滚动真实实现（Load More 可为占位）
6. 推送通知、真实语音/视频通话
7. 多语言文案实际切换（语言选择仅为设置项）
8. 原生 App 打包 / 小程序发布
9. 平台不支持的手势（长按/拖拽）必须提供等效按钮替代，替代本身不判为不合格，但需在交付说明中列出

### 🔍 D. 交付自检清单（提交前逐条核对）

- [ ] 应用一键打开即可用：数据表已建好且种子数据已导入，首屏不出现空数据白板
- [ ] 全部 10 个页面可达、可返回，无死链无死按钮
- [ ] 亮/暗两种主题 × PC/移动两种形态，共 4 组形态预览自检无明显破版
- [ ] 2 个 AI 节点均已配置失败兜底分支并实际验证过
- [ ] 所有写操作（发帖/点赞/评论/私信/删收藏/改资料）刷新后数据仍持久
- [ ] 无任何密钥/敏感信息硬编码在页面或接口配置中（AI 密钥走平台凭证管理）
- [ ] 交付说明中列出：平台能力限制导致的交互替代项清单

---

## 十、给低代码平台的搭建顺序建议

1. 先建 6 张数据表并导入种子数据
2. 配置主题：色板、字体、暗色模式变量；（若支持）加入玻璃拟态自定义 CSS
3. 搭全局壳：PC 顶栏 / 移动底栏 + 抽屉 / Toast / 暗色开关 / currentTab 状态
4. 按 发现页 → 探索牌堆 → 文章详情 → 发布（含 AI 节点）→ 消息（含 AI 节点）→ 关注 → 个人主页 → 设置 → 登录 的顺序搭页面并连接数据表
5. 最后配置 2 个 AI 节点的 Prompt 与兜底分支，按第九章自检清单逐项验证
