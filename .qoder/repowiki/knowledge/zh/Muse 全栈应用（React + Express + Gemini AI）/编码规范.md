- 环境变量统一通过 dotenv 加载，敏感配置（如 GEMINI_API_KEY）从 .env 读取并在 server.ts 中集中初始化客户端。
- 前端模块导入统一使用 `@/` 路径别名指向项目根目录，避免相对路径嵌套。
- 所有 AI 相关逻辑封装在服务端 Express 路由中，前端仅通过 JSON API 调用，不直接依赖 Google GenAI SDK。
- 开发/生产环境通过 NODE_ENV 和 DISABLE_HMR 环境变量区分，Vite HMR 与文件监听按此开关控制。